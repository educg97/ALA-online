import { Route } from '@angular/router';
import { ContestDrawComponent } from './contest-draw.component';
export const ContestDrawRoutes: Route[]=[
    {
        path: '',
        component: ContestDrawComponent
    }
]